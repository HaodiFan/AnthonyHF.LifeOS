# Identities Index

Public-safe scene identity index for AnthonyHF.LifeOS.

Use this directory to define which scene identities this LifeOS can use, what it can represent, what it cannot represent, and when owner confirmation is required. This replaces the old top-level `roles/` bucket, but it does not prescribe fixed role pages for every LifeOS.
