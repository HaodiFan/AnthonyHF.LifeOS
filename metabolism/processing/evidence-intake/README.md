# Evidence Intake Processing

这里放 AnthonyHF.LifeOS 的 evidence first-read、source inventory 和材料处理记录。

这些文件不是长期记忆，也不是 identity 结论。它们只是材料从外部来源进入 LifeOS 后的处理痕迹。

后续路由：

- identity 结论进入 `identity/wenxin/` 或 `identity/psp/`。
- runtime 经验进入 `runtime/memory/working-lessons/`。
- 稳定能力证据进入 `capabilities/*/memory/`。
- 历史或无法分类材料进入 `legacy/`。
