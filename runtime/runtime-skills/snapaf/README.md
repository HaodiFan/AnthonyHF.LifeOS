# snapAF Runtime Skill

`snapAF-skills` 是外部 runtime skill repo 的绑定样本。

它的角色是执行具体任务、产生日志和 lesson；不是 AnthonyHF.LifeOS 的稳定能力本体。当前 LifeOS 只保存 repo manifest、权限边界、输入输出约定和 promotion gate。若多次运行后形成稳定、可复用、跨场景的判断方法，再通过 IPO Reverse 和 owner alignment 升级到 `capabilities/`。

当前 manifest：`manifest.yml`
