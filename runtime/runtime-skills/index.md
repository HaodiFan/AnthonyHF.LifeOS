# Runtime Skills Index

Public-safe runtime skill index for AnthonyHF.LifeOS.

Use this directory for callable or candidate runtime skills. Keep factual context in memory/data-source bindings rather than hard-coding it into skill files.

## Current Runtime Skills

- `snapaf`: external runtime skill repo binding. Manifest: `runtime/runtime-skills/snapaf/manifest.yml`.
