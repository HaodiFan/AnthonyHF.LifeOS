# Docs 展示文档

这一层放对外展示和解释材料。

当前内容：

- `assets/anthonyhf-avatar-iceberg.svg`：README 顶部使用的 repo 架构冰山图，用来让普通人快速看懂 `identity/`、`skills/`、`memory/`、`security/`、`docs/` 的分工。

这里放的是说明材料，不是真相源。身份真相源在 `identity/`，能力真相源在 `skills/`，长期记忆真相源在 `memory/`。
